// A program that combines
// two arrays [1, 2] and [3, 4] into one using the concat() method.

// Declaring variables for th arrays.

let array1 = [1,2];
let array2 = [3,4];

// Variable to add the two Array-variables into one.

let combine = array1.concat(array2);
console.log(combine);
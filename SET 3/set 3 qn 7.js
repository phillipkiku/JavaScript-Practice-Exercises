// A function that reverses a string,
// e.g., "hello" becomes "olleh".

 // Declaring Variable for the string.

let reverseString = "hello";

// Variable to run the string value.

let change = reverseString.split('').reverse().join('');
console.log(change)
